{
  "gsap create context": {
    "prefix": "gcctx",
    "body": [
      "let ${1:ctx} = gsap.context(() => {",
        "${2:animations}",
      "});"
    ],
    "description": "Gsap create Context"
  },
  "gsap create context and add": {
    "prefix": "gcctxadd",
    "body": [
      "let ${1:ctx} = gsap.context(() => {});",
      "${1:ctx}.add(()=>{",
      "${2:animation}",
      "})"
    ],
    "description": "Gsap create Context and add"
  },
  "gsap add to context": {
    "prefix": "gaddctx",
    "body": [
      "${1:ctx}.add(()=>{",
      "${2:animation}",
      "})"
    ],
    "description": "Gsap add to media function"
  },
  "gsap create media": {
    "prefix": "gcm",
    "body": [
      "let ${2:mm} = gsap.matchMedia()",
      "${2:mm}.add(\"(${3:min-width}: ${4:800px})\", () => {",
      "  ${5:animation}",
      "});"

    ],
    "description": "Gsap create media"
  },
  "gsap add media": {
    "prefix": "gmaad",
    "body": [
      "${1:mm}.add(\"(${2:min-width}: ${3:800px})\", () => {",
      "  ${5:animation}",
      "});"
    ],
    "description": "Gsap add to media"
  }
}